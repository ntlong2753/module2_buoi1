public class khaibaobien {
    public static void main(String[] args) {

        // Khai báo biến
        int a;
        byte tuoi;
        float diemToan;

        // Khai báo nhiều biến 1 lúc cùng kiểu;
        int c, d, e, f;

        // Khởi tạo biến
        int tuoiCon = 10; //gán thêm giá trị 10 cho biến tuoiCon
        float diemVan = 7.5f;
        double diemAnh = 7.5;
        tuoiCon = 15;
        System.out.println(tuoiCon);

        // Hằng số
        final int DOSOI = 100; // hằng số là cố định trong suốt chương trình, không thể thay đổi

        // Đặt tên biến
            //Tên biến trong java có phân biệt viết hoa và viết thường
            //Đặt tên theo quy tắc chữ cái đầu từ thứ 2 trở đi viết Hoa, còn lại viết thường

    }
}
